import React from 'react';

const ProfileSelector = () => {
    return (
        <div>
            <h2>Select a Plant Profile</h2>
            {/* Profile selection options will be added here */}
        </div>
    );
};

export default ProfileSelector;